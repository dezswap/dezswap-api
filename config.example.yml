indexer:
  chain_id: cube_47-5
  src_node:
    host: 10.0.0.1
    port: 9090
    factory:
  src_db:
    host: localhost
    port: 5432
    database: dezswap_api
    username: app
    password: appPW
  db:
    host: localhost
    port: 5432
    database: dezswap_api
    username: app
    password: appPW

log:
  level: debug
  format_json: true
  env:
  chain_id:

sentry:
  dsn:

api:
  server:
    name: dezswap-api
    host: 0.0.0.0
    port: 8000
    swagger: false
    mod: release # test, debug, release
    version:
    chain_id:
  db:
    host: localhost
    port: 5432
    database: dezswap_api
    username: app
    password: appPW
